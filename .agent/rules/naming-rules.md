---
trigger: always_on
---

always use camlCase in naming

naming in dto is the same names in the entities.
The database naming strategy will automatically handle conversion to snake_case for columns.
